{
  "name": "example-01-simple-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "ui:build": "cd ui && npm install && npm run build",
    "ui:dev": "cd ui && npm install && npm run dev",
    "start": "node ./index.js",
    "postinstall": "npm run ui:build"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
